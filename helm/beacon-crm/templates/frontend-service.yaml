apiVersion: v1
kind: Service
metadata:
  name: {{ include "beacon.fullname" . }}-frontend
  labels:
    {{- include "beacon.labels" . | nindent 4 }}
    {{- include "beacon.selectorLabels" (dict "context" . "component" "frontend") | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: {{ .Values.frontend.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "beacon.selectorLabels" (dict "context" . "component" "frontend") | nindent 4 }}
